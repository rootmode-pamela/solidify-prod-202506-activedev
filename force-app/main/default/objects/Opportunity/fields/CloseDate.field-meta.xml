<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>CloseDate</fullName>
    <inlineHelpText>Defaults to 1 year from creation if no COE exists (Salesforce requires an input).  Ultimately should match the COE once input, and LOS Closed Date once transaction is complete.</inlineHelpText>
    <trackFeedHistory>true</trackFeedHistory>
    <trackHistory>true</trackHistory>
    <trackTrending>true</trackTrending>
</CustomField>

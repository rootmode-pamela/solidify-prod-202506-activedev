<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Birthday_of_Closed_Date__c</fullName>
    <formula>IF(AND(MONTH(Closed_Date__c)=MONTH(TODAY()),DAY(Closed_Date__c)=DAY(TODAY())), true, false)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Birthday of Closed Date</label>
    <trackHistory>false</trackHistory>
    <type>Checkbox</type>
</CustomField>

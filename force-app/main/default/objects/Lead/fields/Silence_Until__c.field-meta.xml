<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Silence_Until__c</fullName>
    <description>For pipeline view purposes so the Lead shows only when Active.</description>
    <inlineHelpText>For pipeline view purposes so the Lead shows only when Active.</inlineHelpText>
    <label>Silence Until</label>
    <required>false</required>
    <trackFeedHistory>false</trackFeedHistory>
    <trackHistory>false</trackHistory>
    <type>Date</type>
</CustomField>
